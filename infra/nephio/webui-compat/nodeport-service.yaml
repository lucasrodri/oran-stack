apiVersion: v1
kind: Service
metadata:
  name: nephio-webui
  namespace: nephio-webui
spec:
  type: NodePort
  selector:
    app: nephio-webui
  ports:
    - name: http
      protocol: TCP
      port: 7007
      targetPort: http
      nodePort: 30707
