apiVersion: v1
kind: Service
metadata:
  name: gitea-lab
  namespace: gitea
  labels:
    app.kubernetes.io/instance: gitea
    app.kubernetes.io/name: gitea
    app.kubernetes.io/part-of: nephio
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: gitea
    app.kubernetes.io/name: gitea
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30302
