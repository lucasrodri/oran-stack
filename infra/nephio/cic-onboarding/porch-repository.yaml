apiVersion: config.porch.kpt.dev/v1alpha1
kind: Repository
metadata:
  name: cic
  namespace: default
spec:
  content: Package
  deployment: true
  git:
    branch: main
    directory: /
    repo: http://gitea.gitea.svc.cluster.local:3000/nephio/cic.git
    secretRef:
      name: cic-access-token-porch
  type: git
